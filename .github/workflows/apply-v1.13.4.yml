name: Apply v1.13.4 DigitalOcean layout

on:
  push:
    paths:
      - '.github/workflows/apply-v1.13.4.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Apply patch
        run: node scripts/apply-v1.13.4.js
      - name: Validate
        run: |
          node --check lib/client.js
          npm test
      - name: Commit changes
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add lib/client.js package.json CHANGELOG.md docs/releases/v1.13.4.md
          if git diff --cached --quiet; then
            echo 'No generated changes.'
            exit 0
          fi
          git commit -m 'ui: move DigitalOcean account API below billing details'
          git push origin HEAD:main

# trigger: v1.13.4
