# Project History

> 本文记录 `Martin-soaring-dev/dsh-usage-plugin-v2` 在复刻 `feiyang-dev/dsh-usage-plugin` 后的主要修改、设计取舍与版本演进。
>
> 这里关注的是**本 fork 实际新增或调整的功能**，而不是简单复制 Git commit 列表。临时发布脚本、一次性 CI 状态记录等不影响用户功能的提交不单独展开。

## 项目来源

本项目基于 [`feiyang-dev/dsh-usage-plugin`](https://github.com/feiyang-dev/dsh-usage-plugin) 的公开代码复刻，并在此基础上继续维护和增强。

原项目已经提供了 DeepSeek Harness 中较完整的用量与消耗统计能力，包括模型调用记录、Token 用量统计、缓存命中、价格计算、用量日历、数据导入导出以及持久化等功能。

本 fork 的目标不是替代原项目，而是在保留原有核心能力和数据兼容性的基础上，针对实际使用过程中遇到的问题继续补齐功能，尤其包括：

- 更好的跨平台兼容性；
- 更清晰的价格和费用展示；
- 更完整的模型与 API 服务商统计；
- 更适合宽表格和小窗口的界面布局；
- 多服务商余额查询；
- 不同服务商凭据类型与账单语义的明确区分；
- 更完整的中英文文档与真实界面截图。

本项目继续遵循原项目的 MIT License。原始设计、代码及历史贡献归功于原项目作者与贡献者。

## AI 协作声明

本项目属于人机协作开发项目。

本 fork 的需求分析、代码调查、部分实现、测试、调试、问题排查、文档整理、版本发布准备及维护工作，由项目维护者在 **OpenAI Codex / ChatGPT** 的协助下完成。

项目维护者负责提出需求、决定功能方向、提供实际运行反馈、授权涉及账户与凭据的操作、审核修改结果，并对最终合并、发布及软件行为承担责任。AI 的参与不会替代项目维护者的责任；具体代码变更仍以 Git 提交历史为最终依据。

---

# 版本与功能演进

## 2026-08-14：建立 v2 fork

仓库开始在原项目基础上独立维护，保留 DeepSeek Harness 用量与消耗统计插件的核心结构，并开始针对实际使用体验进行调整。

这一阶段奠定了本 fork 的基本原则：

1. 尽量保持与原项目数据结构和使用方式兼容；
2. 优先修复真实使用过程中暴露的问题；
3. 对外部服务商接口只使用可确认的官方能力，不猜测私有或未公开端点；
4. 对费用、余额和凭据等容易产生误解的字段，在界面中明确解释其含义。

### v1.3.2：价格表显示简化

对价格表界面进行了简化。

主要变化：

- 自动价格模式下直接展示当前实际生效的价格表；
- 减少用户理解“基础价 / 自动价 / 当前价”之间关系的成本；
- 保留价格编辑与持久化能力，同时让当前计费规则更直观。

这次修改主要针对价格配置界面信息层级过多、用户不容易立即判断“现在到底按哪个价格计算”的问题。

---

## 2026-08-15：跨平台兼容与文档完善

### v1.3.3：Windows / macOS / Linux 跨平台修复

原有实现中部分路径处理和系统调用带有 Windows 假设。本 fork 对相关逻辑进行了系统性调整。

主要修改包括：

- 使用 `node:path` 统一处理路径拼接和目录名；
- POSIX 路径保持 `/`，避免被错误转换为 Windows 反斜杠；
- 启动子进程时验证 workspace 路径，必要时回退到 `process.cwd()`；
- 针对不同平台选择合适的 Node / Electron 启动方式；
- 原生目录选择器按平台实现：
  - Windows：PowerShell；
  - macOS：`osascript`；
  - Linux：`zenity` / `kdialog`；
- “打开所在目录”按平台实现：
  - Windows：`explorer.exe`；
  - macOS：`open`；
  - Linux：`xdg-open`。

这一版本使插件从“主要按 Windows 环境实现”进一步变为真正可以在 Windows、macOS 和 Linux 上工作的跨平台插件。

同时，在文档和 npm 元数据中补充了对相关 macOS 兼容性贡献者的致谢。

### v1.4.0：文档与真实界面展示

这一阶段继续整理用户文档，并加入真实运行界面截图，包括：

- 用量与消耗主界面；
- 余额查询界面。

安装命令也去除了固定版本号，避免 README 中的命令因版本更新迅速过时。

---

## 2026-08-16：统计视图与界面适配增强

### v1.9.0：Provider × Model 明细与宽表格适配

这一版本对“用量与消耗”页面进行了较大的展示层改进。

主要功能包括：

### 1. API 服务商 × 模型明细

在原有按模型统计之外，增加按 **API Provider × Model** 的拆分视图。

这样可以区分：

- 同一个模型通过不同 API 服务商调用时的使用量；
- 每个服务商下不同模型的调用次数和 Token；
- 高峰 / 空闲时段消耗；
- 服务商与模型组合的费用差异。

这对于同时使用官方 API、第三方中转或多个模型服务商的用户尤其重要。

### 2. 面板宽度自动适配

针对统计表列数增加后容易被窗口裁切的问题，重新调整了布局逻辑：

- 面板宽度以当前 viewport 为上限；
- 宽表格使用 `max-content` 保留完整列宽；
- 容器内允许横向滚动；
- 小窗口下不再直接裁掉右侧字段或合计列。

因此即使统计维度继续增加，也能保证数据完整可见。

### 3. DeepSeek 官方价格说明

补充和整理 DeepSeek 官方价格相关说明，使价格表、峰谷计费和统计结果之间的关系更加明确。

### v1.9.1：README 英文优先与 npm 包名迁移说明

文档结构调整为：

- `README.md`：英文主 README；
- `README.zh.md`：简体中文文档。

同时修正 tarball 测试命令，并将安装文档中的 npm 包引用迁移到：

`@feiyang666/dsh-usage-plugin`

以与原项目后续采用的新 npm 包名保持一致。

---

## 2026-08-18：多服务商余额查询

这是本 fork 相比复刻基线最明显的一组功能扩展。

原余额查询逻辑主要围绕 DeepSeek 展开，但实际使用中常常同时配置多个 API Provider。不同服务商的余额接口、凭据类型和返回字段并不统一，因此不能简单把 DeepSeek 查询逻辑复制到其他服务商。

本项目将余额页扩展为多服务商架构，并针对每个服务商分别处理其认证方式和账单语义。

### v1.10.0：初步加入多服务商余额查询

新增以下服务商的余额入口：

- DeepSeek；
- SiliconFlow；
- DigitalOcean；
- AMD GPU Cloud。

核心变化不是单纯增加选择项，而是让不同 Provider 可以拥有独立的：

- 凭据来源；
- 请求接口；
- 返回字段解析；
- 支持状态说明；
- 安全限制。

同时更新了中英文 README，对多服务商余额支持进行说明。

### SiliconFlow

SiliconFlow 的实现重点解决“到底读取哪一个 API Key”的问题。

本 fork 的行为是：

- 从模型设置中寻找 Provider ID 或显示名为 `siliconflow` 的 Provider；
- 根据该 Provider 配置的 `apiKeyEnv` 读取已经保存的 Key；
- 跟随该 Provider 对应的官方 SiliconFlow API 地址；
- 仅接受 SiliconFlow 官方 `.cn` / `.com` 主机，避免把凭据发送到未知第三方地址；
- 展示公开 API 实际返回的余额字段。

当前页面会如实展示：

- `totalBalance`；
- `chargeBalance`；
- `balance` 等兼容字段。

如果公开 API 返回 0，本项目不会拿网页控制台中的另一个数字强行覆盖，因为两者可能属于不同统计口径。

### DigitalOcean

DigitalOcean 的难点在于：**Gradient AI 推理 API Key 并不等同于 DigitalOcean 账户级 API Token。**

因此，本 fork 没有把推理 Key 错当成账单凭据，而是明确要求用户提供账户级 Personal Access Token（PAT）。

实现包括：

- 在余额页提供 DigitalOcean PAT 输入与保存流程；
- 保存后以隐藏形式显示，不在前端长期展示明文；
- 调用 DigitalOcean 账户 Billing API；
- 展示账户当前余额；
- 展示本月至今使用金额；
- 不额外拉取完整账单历史。

对于 DigitalOcean 返回的负数账户余额，本项目按其记账语义解释为可使用的信用余额，而不是简单显示为“欠费”。

### AMD GPU Cloud

在调查 AMD GPU Cloud 时，没有找到经过公开文档确认、可直接使用推理 API Key 查询 credits / balance 的余额端点。

因此本 fork 明确选择：

- 不猜测接口；
- 不向未知账单端点发送推理 Key；
- 不伪造“支持余额查询”的效果；
- 保留 AMD GPU Cloud 服务商选项；
- 在页面中明确标记为“仅支持控制台查看”；
- 提供官方控制台入口。

这也是本项目处理第三方 Provider 的基本原则：**宁可明确说明暂不支持，也不使用未经确认的接口。**

---

## v1.11.0：余额凭据流程、诊断与 UX 完善

在 v1.10.0 初步加入多 Provider 后，根据实际测试继续修复了多个凭据和交互问题，并在 v1.11.0 中形成较完整的版本。

主要变化包括：

### 1. 完善不同 Provider 的凭据流程

进一步区分：

- 推理 API Key；
- Provider 中通过 `apiKeyEnv` 引用的 Key；
- 账户级 Personal Access Token；
- 不存在公开余额 API 时的“仅控制台查看”状态。

避免所有服务商都套用同一种 Key 输入逻辑。

### 2. 改进余额查询诊断

当查询失败时，尽量给出更具体的原因，而不是统一显示一个模糊的“查询失败”。

重点区分：

- 没有找到对应 Provider；
- Provider 没有配置 `apiKeyEnv`；
- 环境变量中没有对应 Key；
- API 地址不符合官方主机限制；
- 使用了错误类型的 Token；
- 服务商本身没有公开支持余额查询。

### 3. 改进多服务商余额页交互

进一步调整 Provider 切换、凭据说明、查询结果和错误提示，使用户可以直接在界面中理解：

- 当前选的是哪个服务商；
- 该服务商应该使用什么凭据；
- 是否能够通过 API 查询；
- 返回数字的具体含义；
- 如果不能查询，应该去哪里查看。

### 4. 安全边界

余额查询相关的敏感请求继续放在 Host 侧完成。

核心安全原则包括：

- 尽量不把明文凭据返回 Client 前端；
- DigitalOcean PAT 保存后隐藏；
- SiliconFlow 限制为官方主机；
- AMD GPU Cloud 不调用未经官方确认的余额端点；
- 为 Provider 契约和边界补充自动化测试。

---

# README 与项目定位调整

在功能基本稳定后，本 fork 对 README 进行了重新组织。

新版 README 不再直接从原项目介绍开始，而是分为两个部分：

1. **本项目的出发点、做了什么以及与原项目的区别**；
2. **复刻并适配后的原项目介绍**。

其中新增了：

- fork 与原项目关系说明；
- 多服务商余额支持表；
- SiliconFlow / DigitalOcean / AMD GPU Cloud 的实际界面截图；
- AI 协作与维护声明；
- 本项目与原项目功能差异表。

这样可以让后来访问仓库的用户明确知道：哪些能力来自原项目，哪些是本 fork 后续增加或修改的内容。

---

# 当前与原项目复刻基线的主要区别

截至 v1.11.0，本 fork 相比复刻时的原项目基线，主要新增或调整了以下内容：

| 方向 | 本 fork 的主要修改 |
| --- | --- |
| 跨平台 | 系统性修复 Windows / macOS / Linux 的路径、目录选择、目录打开和进程启动逻辑 |
| 价格展示 | 简化自动价格模式，直接展示实际生效价格，并补充 DeepSeek 官方价格说明 |
| 用量统计 | 增加 API Provider × Model 的细分统计 |
| 界面布局 | 宽表格根据 viewport 自适应，并支持容器内横向滚动，避免右侧列被裁切 |
| 文档 | 英文 / 中文 README 分离，补充真实截图、安装说明和 fork 定位 |
| DeepSeek 余额 | 保留原有查询能力 |
| SiliconFlow | 自动读取匹配 Provider 的 `apiKeyEnv`，限制官方主机，解释公开 API 余额字段 |
| DigitalOcean | 使用账户级 PAT 调用 Billing API，展示账户余额与本月至今使用 |
| AMD GPU Cloud | 明确无公开推理 Key 余额端点，不猜测 API，仅提供控制台入口 |
| 凭据安全 | 区分推理 Key 与账户 Token，余额请求留在 Host 侧，敏感凭据尽量不返回前端 |
| 错误诊断 | 对 Provider、Key、Token、Host 和“不支持 API”状态给出更明确提示 |
| 开发透明度 | README 与本文明确声明 OpenAI Codex / ChatGPT 参与开发、测试、文档和维护 |

---

# 发布说明

本 fork 在演进过程中使用 GitHub Release 与 npm 发布流程进行版本准备和验证。

v1.11.0 阶段曾加入一次性发布与状态检查流程，用于处理 GitHub Release / npm 发布问题；这些 CI 辅助提交属于发布基础设施，不代表新的用户功能，因此本文不逐条列出。

需要判断某个版本的精确代码内容时，请以对应 Git tag、GitHub Release 和 commit diff 为准。

---

# 后续维护原则

后续继续修改本 fork 时，建议在本文件中记录具有用户可见影响的功能变化，尤其包括：

- 新 Provider；
- 新统计维度；
- 价格 / 计费规则变化；
- 数据格式变化；
- 凭据获取方式变化；
- 安全边界变化；
- 重要跨平台修复；
- 与上游重新同步时产生的行为差异。

普通重构、格式化、临时 CI、发布状态记录等不影响用户行为的修改，可以只保留在 Git commit history 中，无需重复写入本文。
